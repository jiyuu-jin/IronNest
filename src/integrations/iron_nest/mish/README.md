```bash
cargo run -p mish-cli -- upload-file --mish-state-name run --path fish-tank.rhai src/integrations/iron_nest/mish/fish_tank.rhai
```

```bash
cargo run -p mish-cli -- upload-file --mish-state-name run --path fish-tank-cron.rhai src/integrations/iron_nest/mish/fish_tank_cron.rhai
```
